<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="5dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/first_album_image"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_recovery_page"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/folder_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Folder Name"
                android:textStyle="bold"
                app:layout_constraintLeft_toLeftOf="@+id/first_album_image"
                app:layout_constraintRight_toRightOf="@+id/first_album_image"
                app:layout_constraintTop_toBottomOf="@+id/first_album_image" />

            <TextView
                android:id="@+id/folder_size"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Items: 1000"
                app:layout_constraintLeft_toLeftOf="@+id/folder_name"
                app:layout_constraintRight_toRightOf="@+id/folder_name"
                app:layout_constraintTop_toBottomOf="@+id/folder_name" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>
</layout>